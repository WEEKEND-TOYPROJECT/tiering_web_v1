import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";
import styled from "styled-components";

const Home: NextPage = () => {
  return <Wrapper>1123</Wrapper>;
};

export default Home;
const Wrapper = styled.div`
  background-color: black;
  width: 100px;
  height: 100px;
`;
